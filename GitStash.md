About git stash

- clear all stashes
        ```
        git stash clear
        ```

- list all stashes: 
        ```
        git stash list
        ```
- stash new file:
        ``` git stash --include-untracked ```
        
- restore change from stash: ``` git stash apply ```
